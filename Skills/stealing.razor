while skill "Stealing" < 100
    if weight >= 136
        sysmsg "Moving back to llama."
        wait 30000
        organizer 20
    endif
    
    wait 6000
    useskill "Stealing"
    wft
    target 0x1C3663
    wait 5000
endwhile